/**
 * 결제 페이지 하단 '자주 묻는 질문'.
 *
 * `profile/constants/faq.ts` 와 같은 규칙을 따른다: **실제 동작에 근거해서만 적는다.**
 * 결제 직전 화면이라 여기서 틀리면 사용자가 돈을 낸 근거가 틀린 게 된다.
 *
 * 각 항목에 근거를 주석으로 남겼다. 근거를 못 대는 문장은 넣지 않는다 — 특히:
 *
 * ⚠️ **'무료 체험'을 쓰지 않는다.** `PaymentConfirmSheet` 에 '무료 체험 3일' 줄이 남아
 *    있지만 그건 시안에서 옮겨온 미검증 값이고(같은 파일 주석 참고), 응답 어디에도
 *    체험 관련 필드가 없다. 검증되기 전까지 다른 자리로 퍼뜨리지 않는다.
 *
 * ⚠️ **'상위 요금제로 변경'을 쓰지 않는다.** 종전 페이지 하단에 그 문구가 있었는데
 *    요금제를 바꾸는 엔드포인트가 없다 — `api/paymentApi.ts` 에 있는 건 시작·해지·재개·
 *    결제수단뿐이다. 바꾸려면 해지 후 재구독이라 "언제든 변경" 과 동작이 다르다.
 *
 * 숫자(용량·횟수)는 적지 않는다. 요금제마다 다르고 서버가 바꿀 수 있어서, 값은 바로 위
 * 비교표가 말하게 두고 여기서는 규칙만 설명한다.
 */

export interface PremiumFaqItem {
  q: string;
  a: string;
}

export const PREMIUM_FAQ: PremiumFaqItem[] = [
  {
    // 근거: billingCycle 'MONTHLY' + 빌링키 결제(useBillingCheckout → registerBillingKey).
    q: '결제는 어떻게 이뤄지나요?',
    a: '카드를 한 번 등록해 두면 매월 같은 날 자동으로 결제돼요. 결제가 끝나면 늘어난 용량과 횟수가 바로 적용돼요.',
  },
  {
    // 근거: cancelSubscription — status 는 ACTIVE 로 남고 autoRenew 만 false 가 된다.
    q: '구독을 해지하면 바로 끊기나요?',
    a: '아니요. 자동 갱신만 꺼지고, 이미 결제한 기간이 끝날 때까지는 그대로 쓸 수 있어요. 그 뒤에 무료 플랜으로 돌아가요.',
  },
  {
    // 근거: resumeSubscription — 만료 전이면 autoRenew 를 되돌려 nextBillingAt 이 복귀한다.
    q: '해지했다가 마음이 바뀌면요?',
    a: '이용 기간이 끝나기 전이라면 마이페이지의 구독 및 결제에서 자동 갱신을 다시 켤 수 있어요. 결제가 한 번 더 나가지 않고 그대로 이어져요.',
  },
  {
    // 근거: getBillingKeys / deleteBillingKey 를 SubscriptionPage 가 쓴다.
    q: '등록한 카드는 어디서 바꾸나요?',
    a: '마이페이지의 구독 및 결제에서 등록된 카드를 확인하고 지울 수 있어요.',
  },
  {
    // 근거: 무료 플랜도 /subscription-plans 에 있고 features 가 용량·AI 블로그에만 걸린다.
    // 기록·동선에 제한이 없다는 건 profile/constants/faq.ts 와 같은 근거.
    q: '무료로는 어디까지 쓸 수 있나요?',
    a: '장소를 기록하고 동선을 만드는 데에는 제한이 없어요. 사진 저장 용량과 AI 블로그 횟수에만 제한이 있고, 위 비교표에서 플랜별로 확인할 수 있어요.',
  },
];
