/**
 * 페이지 맨 위 유도 문구.
 *
 * **네비바와 헤드라인 사이를 80px 비운다**(참고: 유튜브 프리미엄도 같은 값). 이 페이지는
 * 스크롤 첫 화면이 곧 표지라, 위가 빌수록 아래 글이 광고가 아니라 안내로 읽힌다. 앱의
 * 다른 화면들이 헤더 바로 아래에서 본문을 시작하는 것과 일부러 다르게 둔 자리다.
 *
 * 워드마크는 한때 있다가(앱 아이콘 + 한 줄) 빠졌던 것을 글자만 되살린 것이다. 그림을
 * 다시 붙일 거라면 `public/apple-touch-icon.jpg`(앱 아이콘)를 쓴다 — 런처·홈 화면에서
 * 이미 그 그림으로 이 앱을 아는 사람이 보는 화면이고, 디자인 규칙의 GREEN-500·700 도
 * 그 파일에서 뽑은 값이라 배경 그라데이션과 같은 계열로 앉는다.
 *
 * 헤드라인은 이 페이지에서 제일 큰 글자다. **굵기는 medium** — 한때 `font-bold` 로
 * 뒀다가 되돌렸다. §2 의 '볼드 금지' 를 지키는 쪽이고, 크기(27px)만으로도 위계는 선다.
 */
export function PremiumHero() {
  return (
    // 안전영역 여백은 위의 `NavBar` 헤더가 가져갔다 — 여기서 `pt-header` 를 또 주면
    // 노치 높이만큼 두 번 밀린다.
    <div className="flex flex-col items-center px-5 text-center">
      {/*
        워드마크. **글자는 대소문자 그대로 두고 CSS 로만 대문자로 만든다** —
        마크업에 'PICTREE PREMIUM' 이라고 박으면 스크린리더가 낱자로 읽는 경우가 있다.

        자간을 벌리는 건 대문자라서다. 한글·소문자와 달리 대문자는 글자마다 세로획이
        나란히 서서 기본 자간으로는 덩어리져 보인다. 0.12em 이면 낱말로 흩어지지 않으면서
        표지 글자처럼 읽힌다. ⚠️ 아래 헤드라인의 `tracking-[-0.01em]`(음수)와 반대 방향인
        게 맞다 — 저쪽은 27px 큰 글자라 오히려 조여야 한 덩어리로 읽힌다.
      */}
      <p className="mt-20 text-[15px] font-medium uppercase tracking-[0.12em] text-pictree-700">
        PicTree Premium
      </p>

      <h1 className="mt-3 text-[27px] font-medium leading-[1.35] tracking-[-0.01em] text-ink">
        나만의 순간과 기록을
        <br />
        제한없이 남겨보세요
      </h1>
    </div>
  );
}
